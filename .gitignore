# Python-generated files
__pycache__/
*.py[oc]
*.pyc
build/
dist/
wheels/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/

# Environment / secrets — NEVER commit
.env
.env.*
!.env.example

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# HuggingFace / model caches (downloaded at runtime)
.cache/

# OS / editor
.DS_Store
Thumbs.db

# Gradio's local cert cache (created by launch(share=True))
.gradio/

# Project data
# NOTE: the vector store now lives in Pinecone (see core/config.py), not on
# disk, so neither the scraped knowledge-base .md files nor the old local
# Chroma store need to ship with the repo or the HF Space anymore.
data/

# Internal docs — not published to GitHub/HF
PROJECT_SPEC.md

archify/
